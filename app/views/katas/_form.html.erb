<%= form_for @kata, :url => { :controller => "katas", :action => "create" } do |f| %>
  <% if @kata.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kata.errors.count, "error") %> prohibited this kata from being saved:</h2>

      <ul>
      <% @kata.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :instructions, 'Instructions for the kata: ' %><br />
    <%= f.text_area :instructions, :size => "80x30" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
